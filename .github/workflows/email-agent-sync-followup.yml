# Gmail sent → "Email Agent Follow Up" tab + warm-up reply promotion + orphan-draft reconcile.
# Schedule: every hour (`cron: '0 * * * *'`).
#   1. sync_email_agent_followup.py — refreshes Email Agent Follow Up from Gmail sent mail
#      (also flips Email Agent Drafts row status pending_review → sent for any row whose
#      gmail_message_id is in Follow Up — closes the manual-Gmail-send drift loop).
#   2. suggest_warmup_prospect_drafts.py --reply-promotion-only — promotes AI: Warm up prospect →
#      AI: Prospect replied when Gmail shows inbound from the prospect after the last logged send.
#      Also tags the inbound message with the AI/Prospect Replied Gmail label.
#   3. reconcile_orphan_reply_drafts.py — for any Gmail draft on a thread that already wears
#      AI/Prospect Replied (operator hit Reply manually in Gmail UI), apply the label to the
#      outbound draft + append an Email Agent Drafts row so the DApp Prospects tab surfaces it.
# Code path: `scripts/sync_email_agent_followup.py` on the **default branch** — merge PRs that
# change that script before expecting new columns (e.g. body_plain).
# Optional one-time: run locally or add workflow_dispatch input `backfill_body_plain` to fill empty body_plain.
# Pair with manager-followup-drafts.yml for draft generation (daily).
#
# Secrets: GOOGLE_CREDENTIALS_JSON, GMAIL_TOKEN_JSON
name: Email Agent Follow Up sync

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run — no Sheet writes'
        type: boolean
        default: false
      backfill_body_plain:
        description: 'After sync, backfill empty body_plain via Gmail (uses quota; one-time catch-up)'
        type: boolean
        default: false
  schedule:
    - cron: '0 * * * *'

jobs:
  sync-follow-up:
    name: Sync Email Agent Follow Up
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create Google credentials file
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
        run: |
          echo "$GOOGLE_CREDENTIALS_JSON" > google_credentials.json

      - name: Sync Email Agent Follow Up (Gmail sent → sheet)
        env:
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
          INPUT_BACKFILL: ${{ github.event.inputs.backfill_body_plain }}
        run: |
          if [ "${INPUT_DRY_RUN:-false}" = "true" ]; then
            python scripts/sync_email_agent_followup.py --dry-run
            exit 0
          fi
          python scripts/sync_email_agent_followup.py
          if [ "${INPUT_BACKFILL:-false}" = "true" ]; then
            python scripts/sync_email_agent_followup.py --backfill-body-plain --backfill-only
          fi

      - name: Handle bounces (clear dead emails, re-queue contact discovery)
        # Delivery failures route the Hit List row back to AI: Enrich with
        # contact (bounced address excluded from re-discovery via Notes
        # marker) and discard any staged drafts to the dead inbox.
        # See agentic_ai_context/WARMUP_AUTOSEND_PLAN.md (PR4).
        env:
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          if [ "${INPUT_DRY_RUN:-false}" = "true" ]; then
            python scripts/handle_warmup_bounces.py
          else
            python scripts/handle_warmup_bounces.py --execute
          fi

      - name: "Promote warm-up replies (AI: Warm up prospect → AI: Prospect replied)"
        env:
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          if [ "${INPUT_DRY_RUN:-false}" = "true" ]; then
            python scripts/suggest_warmup_prospect_drafts.py --reply-promotion-only --dry-run
          else
            python scripts/suggest_warmup_prospect_drafts.py --reply-promotion-only
          fi

      - name: Reconcile orphan reply drafts (manual Gmail Reply → AI/Prospect Replied label + sheet row)
        env:
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          if [ "${INPUT_DRY_RUN:-false}" = "true" ]; then
            python scripts/reconcile_orphan_reply_drafts.py --dry-run
          else
            python scripts/reconcile_orphan_reply_drafts.py
          fi

      - name: Detect follow-up replies (Manager Follow-up / Bulk Info Requested)
        env:
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          if [ "${INPUT_DRY_RUN:-false}" = "true" ]; then
            python scripts/suggest_manager_followup_drafts.py --reply-promotion-only --dry-run
          else
            python scripts/suggest_manager_followup_drafts.py --reply-promotion-only
          fi

      - name: Stage reply response drafts + notify operator (reply acceleration)
        # For every AI/Prospect Replied thread where the prospect spoke last and
        # no response draft exists: stage a Grok-drafted reply in-thread and
        # email the operator. Drafts are NEVER auto-sent — replies stay human.
        # See agentic_ai_context/WARMUP_AUTOSEND_PLAN.md (PR2).
        env:
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          GROK_API_KEY: ${{ secrets.GROK_API_KEY }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          if [ "${INPUT_DRY_RUN:-false}" = "true" ]; then
            python scripts/draft_prospect_reply_responses.py
          else
            python scripts/draft_prospect_reply_responses.py --execute
          fi

      - name: Classify replies + auto-park unambiguous negatives
        # soft_no_with_date -> Deferred / Revisit later (+ Follow Up Date),
        # soft_no -> On Hold, hard_no -> Rejected; referral names -> Sales
        # Process Notes. Only rows still in automated-outreach statuses are
        # parked; every classification leaves a DApp Remarks audit row.
        # See agentic_ai_context/WARMUP_AUTOSEND_PLAN.md (PR3).
        env:
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          GROK_API_KEY: ${{ secrets.GROK_API_KEY }}
          INPUT_DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          if [ "${INPUT_DRY_RUN:-false}" = "true" ]; then
            python scripts/classify_warmup_replies.py
          else
            python scripts/classify_warmup_replies.py --execute
          fi
